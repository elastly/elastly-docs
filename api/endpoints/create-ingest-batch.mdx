---
title: "Open a batch"
sidebarTitle: "Open a batch"
description: "Open a staged ingest batch to push canonical records into."
openapi: "POST /api/v1/ingest/batches"
playground: "simple"
---

<Note>
  This endpoint writes to your workspace, so the interactive playground is turned off for it. Copy
  the request and run it against your own workspace with a connector-scope key.
</Note>
