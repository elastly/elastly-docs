---
title: "Stage records"
sidebarTitle: "Stage records"
description: "Stage up to 1000 canonical records into an open batch."
openapi: "POST /api/v1/ingest/batches/{batchId}/records"
playground: "simple"
---

<Note>
  This endpoint writes to your workspace, so the interactive playground is turned off for it. Copy
  the request and run it against your own workspace with a connector-scope key.
</Note>
